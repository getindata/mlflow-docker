# MLflow docker image with PostgreSQL, AWS and Google Cloud support


Images can be found in `gcr.io/getindata-images-public/mlflow-gcp-aws:*` public repository.

Release tags follow mlflow versions.

